import { Box } from '@shared/ui';
import HomeHeader from '@widgets/home-header/ui/HomeHeader';

export const HomePage = () => {
  return (
    <Box className="flex-1 bg-gray-0">
      <HomeHeader />
    </Box>
  );
};
